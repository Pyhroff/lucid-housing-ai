# Warm, premium, LIGHT theme — calm and trustworthy for a stressed user.
[theme]
base = "light"
primaryColor = "#c2613f"               # warm terracotta (buttons / CTAs)
backgroundColor = "#faf7f2"            # warm cream canvas
secondaryBackgroundColor = "#f2ebe1"   # warm panel / sidebar
textColor = "#2c2722"                  # warm near-black
